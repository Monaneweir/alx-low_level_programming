#include "main.h"

/**
 *_memcpy - Entry point 
* @dest: input
* @src:  input
* @n: input
* Return: Always 0(Success) 
*/
char *_memset(char *s, char b, unsigned int n);
{
     
	unsigned int i;
	for (i=0;i < n;i++) 
	dest [i]=src [i];
	    return (dest);
}
